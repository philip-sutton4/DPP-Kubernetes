tempo:
  retention: 24h
  storage:
    trace:
      backend: local
      local:
        path: /var/tempo/traces

persistence:
  enabled: true
  size: 10Gi

service:
  type: ClusterIP
  port: 4318 # OTLP HTTP receiver
